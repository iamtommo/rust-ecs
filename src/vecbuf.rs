// indirect vector buffers
// useful for storing a reference to a dynamic buffer in components

pub struct VecHandle {
    pub index: usize
}

pub(crate) struct VecBuf {
    //pub(crate) buf: HashMap<VecHandle, Vec>
}
impl Default for VecBuf {

}